/**
 * Naming Conventions
 * 
 * 1)일반적으로 영어를 사용하며 문자와 숫자를 모두사용가능
 * 2) 특수기호는 언더스코어와 달러를 사용
 * 3) 숫자로 이름을 시작할 수 없음
 *      1name, 2hello
 * 4) 키워드는 변수명으로 사용할 수 없다.
 *  var const = 'var';
 * 
 */

let $ive = '아이브'

/**
 * naming convention 2
 * 
 * 1) camelCase - 대부분의언어에서많이사용, 첫문자소문자그다음부턴대문자
 * 2) snake_case - 언더바로 구분, 파이썬에서많이사용
 * 3) PascalCase - C# 마이크로소프트 계열의 언어에서 사용
 */

const anYuJin = '안유진';
console.log(anYuJin);